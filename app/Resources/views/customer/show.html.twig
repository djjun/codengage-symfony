{% extends 'base.html.twig' %}

{% block title %}Clientes - {{ customer.name }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="jumbotron">
                    <h1>{{ customer.name }}</h1>
                    Data de Nascimento: <strong>{{ customer.bornAt | date('d/m/Y') }}</strong>
                </div>
            </div>
        </div>
        <a href="{{ path('customers.index') }}">Voltar</a>
    </div>
{% endblock %}
